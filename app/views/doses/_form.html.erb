<%= simple_form_for [@cocktail, @dose = Dose.new] do |f| %>
  <% if @dose.errors.any? %>
    <div class="errors-container">
      <ul>
        <% @dose.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<!--   <%= f.association :cocktail %>
 -->  <%= f.input :description, placeholder: 'Specify what you need to add: cl, leaves, part ...' %>
  <%= f.association :ingredient %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
